import React from 'react';
import Router from './router';

// import Main (shell)
const App = () => <Router />;

/* function App() {
    return (
      <div id="root">
        WORKINGS
        <TempComponent/>
      </div>
    );
  } */

export default App;
